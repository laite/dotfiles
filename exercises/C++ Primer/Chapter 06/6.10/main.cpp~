// Ex 6.10: fixing errors

#include "../Ch6headers.h"

int main()
{
	// (a): missing break; after items
	// (b): cant initialize variables unless in {} - block
	// (c): multiple case not allowed wiht comma
	// (d): must be const-items in testcases!

	return 0;
}
