// Ex 6.16: prefix of a vector

#include "../Ch6headers.h"

int main()
{
	vector<int> iVec1;

	for (int i = 0; i != 10; ++i)
		iVec1.push_back(i);

	vector<int> iVec2(iVec1);
	iVec2.push_back(13);
	iVec2.push_back(14);

	return 0;
}
