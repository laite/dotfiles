// Ex 3.16: 3x42

#include "../Ch3headers.h"

int main()
{
	cout << "* Example begins *" << endl;

	vector<int> a(10);
	vector<int> b(10,42);
	vector<int> c;

	for (int i = 0; i < 10; ++i){
		a[i] = 42;
		c.push_back(42);
	}

	return 0;
}
