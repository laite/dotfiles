// Ex 7.30: inline functions

#include "../Ch7Headers.h"

inline string &get_shorter(string &s1, string &s2)
{
	return (s1.size() < s2.size()? s1 : s2);
}

int main()
{
	string s1("short"), s2("much longer!");

	cout << "Short string is " << get_shorter(s1, s2) << endl;

	return 0;
}
