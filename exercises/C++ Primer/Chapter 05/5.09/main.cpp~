// Ex 5.09: expressions

#include "../Ch5headers.h"

int main()
{
	int a = 2, b = 4, c = 4, d = 8;

	if (a < b && b < c && c < d)
		cout << "Seems to be so." << endl;
	else
		cout << "Nope. " << endl;

	return 0;
}
